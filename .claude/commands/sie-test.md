# /sie-test - Run SIE parser tests

Run only the SIE4 parser tests with verbose output.

```bash
cd /home/lasse/Documents/Onboarding_App/tic-tac-toe-app/backend
./venv/bin/pytest tests/test_sie_parser.py -v
```

Show which tests pass/fail and any error details.
