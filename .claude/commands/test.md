# /test - Run all tests

Run the full test suite for the backend accounting module.

```bash
cd /home/lasse/Documents/Onboarding_App/tic-tac-toe-app/backend
./venv/bin/pytest tests/ -v
```

Show test results and report any failures.
