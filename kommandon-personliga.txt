eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_rsa_celestial_do
# Ange din passphrase (BP8UC-ET9HX) om du blir frågad igen.

ssh root@46.101.221.97
# Ange passphrase för nyckeln om det behövs (den borde vara i agenten).
# Svara 'yes' på frågan om serverns identitet om det dyker upp igen.

su - yourusername

ps aux | grep gunicorn

#Kör detta för att se om stripe_project fortfarande körs:
ps aux | grep flask

tail -f nohup.out

sudo netstat -tlnp | grep :80
sudo netstat -tlnp | grep :443

sudo ss -tuln | grep :80
sudo ss -tuln | grep :443

#Lösen för yourusername
W5X9X-YVDMC

#Tvinga skriptkörning
cd .git/hooks
./post-merge

# Aktivera den virtuella miljön
source venv/bin/activate

# Starta servern
export FLASK_APP=server.py
python -m flask run --port=4242 --host=0.0.0.0



echo -n '1fc1c3bb-79d0-4b39-b541-70ef67c810a1:c96cdfe6-84d8-477a-a872-ab93c6e89203' | base64
MWZjMWMzYmItNzlkMC00YjM5LWI1NDEtNzBlZjY3YzgxMGExOmM5NmNkZmU2LTg0ZDgtNDc3YS1h
ODcyLWFiOTNjNmU4OTIwMw==

curl -k -d "grant_type=client_credentials" \
     -H "Authorization: Basic MWZjMWMzYmItNzlkMC00YjM5LWI1NDEtNzBlZjY3YzgxMGExOmM5NmNkZmU2LTg0ZDgtNDc3YS1hODcyLWFiOTNjNmU4OTIwMw==" \
     https://api.roaring.io/token